REVISÃO
  MÉTODOS DE BUSCA (cega, sem informação ou força bruta)
    > Técnicas de IA para "Motores" de Raciocínio
      - Raciocínio humano é Dedutivo

    1. Profundidade
      - Visitados, Pilha (recursão)

    2. Largura ou Amplitude (completo)
      - Visitados, fila
      - 1º Estados (estrutura de dados)
      - 2º Regras de Transição (métodos)
      - 3º Restrições
      - 4º Visitados (string)
      - 5º Função Objetivo

18/03/2026
  • Heurística ("dica" ou "com informação")
    - "Dica" precisa de um especialísta (humano ou matemática)
    - "Dica" pode ser furada
    - Dois tipos de Custos
      > Real - g(n)
      > Estimado - h(n)

    3. Subida da Encosta (Climb Hill)
       > Profundidade (usa custo real)

    4. Guloso (Greedy)
      > Amplitude (usa custo estimado)

    5. A*
      > Amplitude (com correção do custo estimado)
        - A correção é a soma do custo real acumulado + o custo estimado

---

PROLOG (usando Profundidade)

conecta(ss,sm,64).
conecta(sm,it,18).
conecta(sm,sil,24).
conecta(sm,sp,60).
conecta(it,jc,40).
conecta(sp,jc,200).
conecta(jc,sil,90).

caminho(O,D,_) :- conecta(O,D,_).
caminho(O,D,_) :- conecta(0,I,),
caminho(I,D,_).


progenitor(alex,raimundo).
progenitor(raimundo, eduardo).
progenitor(eduardo,matheus).
progenitor(eduardo,joao).

progenitor(adolfo,marzari).
progenitor(adolfo,livia).
progenitor(aldolfo_senior, adolfo).

avo(A,N) :- progenitor(A,P),
progenitor(P,N).

ascendente(A,D) :- progenitor(A,D).
ascendente(A,D) :- progenitor(A,I),
ascendente(I,D).

avo(A,N) :- progenitor(A,P),
progenitor(P,N).
